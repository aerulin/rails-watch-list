<div class="container">
  <ul class="list-group">
    <% @lists.each do |list| %>
      <li class="list-group-item d-flex align-items-center">
        <span class="badge badge-primary badge-pill">14</span>
        <%= list.name%>
        <%= link_to "See more", list_path(list), class:"btn btn-secondary"%>
        <%= link_to "Remove",
            list_path(list),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
      </li>

    <% end %>
  </ul>

  <%= link_to "➕ Create a new list", new_list_path, class:"btn btn-secondary py-2 px-5" %>
  <%= link_to "🎬 Create a new film", new_movie_path, class:"btn btn-secondary py-2 px-5" %>
  <%= link_to "🎬 See all movies", movies_path, class:"btn btn-secondary py-2 px-5" %>
</div>
