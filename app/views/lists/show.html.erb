<div class="container">

<div>
<h1><%= @list.name %></h1>
<ul>
<% @bookmarks.each do |bk| %>
<%movie = Movie.find(bk.movie_id)%>
<li><%= bk.comment%> - <%= movie.title %></li>
<% end %>
</ul>

<%= link_to "➕ add a bookmark", new_list_bookmark_path(@list)  %>
</div>

<%= link_to 'Back', root_path %> |

</div>
