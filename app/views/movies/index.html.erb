<div class="banner">

</div>

<div class="container d-flex flex-wrap justify-content-around">
  <% @movies.each do |movie| %>
    <div class="card flex-row" style="width: 500px; height: 300px; margin:10px">
      <img class="card-img-to img-fluid" src=<%= movie.poster_url %> alt="Card image cap">
      <div class="card-body d-flex flex-column justify-content-between">
        <h5 class="card-title"><%= movie.title %></h5>
        <p class="card-text"><%= movie.overview %></p>
        <a href="#" class="btn btn-primary ">Go somewhere</a>
      </div>
    </div>
    <% end %>
</div>

<div class="container d-flex justify-content-center">
  <%= link_to "🎬 Create a new film", new_movie_path, class:"btn btn-secondary py-2 px-5 m-3" %>
  <%= link_to "📕 Back to lists", root_path, class:"btn btn-secondary py-2 px-5 m-3" %>
</div>
